# case-assignment

## Build & Run

- Install [Rust](https://www.rust-lang.org/tools/install).
- Open a terminal in the root directory of this project, and enter the following command:
  ```
  cargo run
  ```
